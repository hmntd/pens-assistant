name: CRM CI (Bypass)

on:
  pull_request:
    paths-ignore:
      - 'crm/**'

permissions:
  contents: read

jobs:
  crm-test:
    runs-on: ubuntu-latest
    steps:
      - name: Skip CI
        run: echo "No changes in crm/. Skipping CRM CI to unblock the PR."